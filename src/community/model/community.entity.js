export class CommunityItem {
    constructor(id, name, members, active_users) {
        this.id = id;
        this.name = name;
        this.members = members;
        this.active_users = active_users;
    }
}